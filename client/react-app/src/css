/* Styling for the rectangle (drop area) */
.ImageRect {
  width: 400px;
  height: 300px;
  border: 2px dashed lightgray;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  background-color: #f9f9f9;
  position: relative; /* Relative positioning for file input */
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.ImageRect.dragging {
  background-color: #e0ffe0; /* Background color when dragging */
}

/* Icon styling */
.ImageIcon {
  margin-bottom: 20px;
}

/* Styling the uploaded image preview */
.uploaded-image {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
}

/* Styling for the instruction text */
.InstrText p {
  margin: 0;
  color: #555;
}

/* Invisible file input covering the rectangle */
.file-input {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0; /* Make it invisible */
  cursor: pointer;
}
